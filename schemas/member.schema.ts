export default interface Member {
   name: string
   slug: string
   image: any
   bio: string
   role: string
   email?: string
   phone?: string
   website?: string
   links?: string[] //todo temporary
}
